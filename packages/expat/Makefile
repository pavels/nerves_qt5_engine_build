PACKAGE_NAME=expat
PACKAGE_VERSION=2.2.9

PACKAGE_URL=https://github.com/libexpat/libexpat/releases/download/R_2_2_9/expat-$(PACKAGE_VERSION).tar.gz
PACKAGE_FILE=expat-$(PACKAGE_VERSION).tar.gz
PACKAGE_ARCHIVE_DIR=expat-$(PACKAGE_VERSION)

CONFIGURE_PARAMS=--host=$(GNU_HOST_NAME) --prefix=$(ENGINE_PATH)

include $(BASEDIR)/package.mk
