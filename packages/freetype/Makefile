PACKAGE_NAME=freetype
PACKAGE_VERSION=2.10.2

PACKAGE_URL=https://download.savannah.gnu.org/releases/freetype/freetype-$(PACKAGE_VERSION).tar.gz
PACKAGE_FILE=freetype-$(PACKAGE_VERSION).tar.gz
PACKAGE_ARCHIVE_DIR=freetype-$(PACKAGE_VERSION)

CONFIGURE_PARAMS=--host=$(GNU_HOST_NAME) --prefix=$(ENGINE_PATH) --without-png

include $(BASEDIR)/package.mk
