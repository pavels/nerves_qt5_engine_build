PACKAGE_NAME=nspr
PACKAGE_VERSION=4.27

PACKAGE_URL=https://archive.mozilla.org/pub/nspr/releases/v$(PACKAGE_VERSION)/src/nspr-$(PACKAGE_VERSION).tar.gz
PACKAGE_FILE=nspr-$(PACKAGE_VERSION).tar.gz
PACKAGE_ARCHIVE_DIR=nspr-$(PACKAGE_VERSION)/nspr

CONFIGURE_PARAMS=--target=$(GNU_HOST_NAME) --prefix=$(ENGINE_PATH)

include $(BASEDIR)/package.mk
