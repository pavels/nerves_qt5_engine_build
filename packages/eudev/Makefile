PACKAGE_NAME=eudev
PACKAGE_VERSION=3.2.9

PACKAGE_URL=https://dev.gentoo.org/~blueness/eudev/eudev-$(PACKAGE_VERSION).tar.gz
PACKAGE_FILE=eudev-$(PACKAGE_VERSION).tar.gz
PACKAGE_ARCHIVE_DIR=eudev-$(PACKAGE_VERSION)

CONFIGURE_PARAMS=--host=$(GNU_HOST_NAME) --prefix=$(ENGINE_PATH) --disable-selinux --disable-blkid

include $(BASEDIR)/package.mk
