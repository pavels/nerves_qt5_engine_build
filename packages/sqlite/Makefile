PACKAGE_NAME=sqlite
PACKAGE_VERSION=3320300

PACKAGE_URL=https://www.sqlite.org/2020/sqlite-autoconf-$(PACKAGE_VERSION).tar.gz
PACKAGE_FILE=sqlite-autoconf-$(PACKAGE_VERSION).tar.gz
PACKAGE_ARCHIVE_DIR=sqlite-autoconf-$(PACKAGE_VERSION)

CONFIGURE_PARAMS=--host=$(GNU_HOST_NAME) --prefix=$(ENGINE_PATH) --disable-editline --disable-readline

include $(BASEDIR)/package.mk
